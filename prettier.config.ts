import { type Config } from "prettier";

const config: Config = {
  trailingComma: "none",
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  printWidth: 80,
  arrowParens: "always",
  proseWrap: "always",
  endOfLine: "lf",
};

export default config;
